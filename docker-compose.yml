version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///data/momboard.db
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; r = httpx.get('http://localhost:8000/healthz'); exit(0 if r.status_code == 200 else 1)"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  # Optional: Postgres for production-like local dev
  # Uncomment and set DATABASE_URL=postgresql+asyncpg://momboard:secret@db:5432/momboard
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: momboard
  #     POSTGRES_PASSWORD: secret
  #     POSTGRES_DB: momboard
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U momboard"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   pgdata:
